# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS
.DS_Store

# Skill output — user audio analyses, stems, rendered widgets (never commit)
track-coach-output/
*-output/
output/
stems/
*.html

# Audio / project files (user data — never commit)
*.wav
*.mp3
*.m4a
*.aiff
*.flac
*.als

# …EXCEPT committed test fixtures: derived analysis JSON + the model's read (no music), and ONE tiny
# synthetic clip for the analyze→json smoke test. These travel with the repo so tests run anywhere.
!tests/fixtures/
!tests/fixtures/**

# Logs / temp
*.log
*.tmp
*.bak

# Internal dev/resume state (not part of the released skill)
NEXT_STEPS.md

# Internal working agreement (dev process, not the released skill)
PLAYBOOK.md

# Internal engineering/design diary — local only (candid notes; repo is public)
JOURNAL.md

# Session scratch / working notes under data/ — resume trail, kept on disk, not committed.
# (Real data under data/ is JSON: reference_*.json, facet_confirmation.json — those stay tracked.)
data/*.md
/catalog.json

# harness worktree/isolation state — never commit
.claude/
