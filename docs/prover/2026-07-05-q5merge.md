# Prover record — Q5 merge (one reference panel), CROSS-LINK mode — 2026-07-05 (s58)

Scope: the §D.10.1/.2/.3 rewrite merging `#refRead` + `#webPanel` into one container `#refPanel`
(shared selector, two nested open disclosures; web notes follow the selector, embedded for all ≤3
directions). Mode: CROSS-LINK — the restructured surface's seams against every named axis. Previous
record: `2026-07-05-pass3.md` (no unfolded rows).

| # | Seam / claim | Verdict | Folded / rejected |
|---|---|---|---|
| Q5-1 | × view ladder (INV-18/22): Simple must hide the CONTAINER; nested ids hidden with it. CSS today hides `#refRead`/`#webPanel` separately — must move to `#refPanel` (or keep all three; container suffices). Ladder/hide-set test expectations name ids — update. | must-fix (code+test) | FOLDED — spec §B.15 + §I.10 updated to `#refPanel`; matrix row + test update queued in the same landing |
| Q5-2 | × references switch (D-INV-23): the one switch must hide `#refPanel` as one unit, not two panels. | must-fix (code) | FOLDED — spec §D.10.2 compose paragraph; code step checks the switch's hide list |
| Q5-3 | × INV-46 registry: new tc-panel id `refPanel` must be registered or DOM-scan goes red; nested `refRead`/`webPanel` stay registered (the arrPanel precedent). Gate tests: refPanel populated; refRead populated; webPanel populated AND follows selector; absent-on-plain negative moves to refPanel. | must-fix (test) | FOLDED — spec scope-split table rewritten; matrix step adds rows |
| Q5-4 | × INV-47 (no empty-open): web disclosure OPEN by default ⇒ a focused direction with no web content must HIDE the disclosure, never render it empty-open. Composed across the selector (a hide that re-shows on switch-back). | must-fix (spec+code+test) | FOLDED — spec'd in §D.10.2 (liveness × selector); browser test queued |
| Q5-5 | × D-INV-30 read order: centroid before web INSIDE the panel; panel sits after tonal balance. | holds | — (spec restated it; existing order test updates id) |
| Q5-6 | × D-INV-28: tabs stay ephemeral; recompute fallback-to-nearest is build-time (rebuild re-renders default=nearest). | holds | — |
| Q5-7 | × viewport (P3-1): `#refPanel` joins the narrow-viewport browser guard. | must-fix (test) | FOLDED — §I.10 updated; test extends |
| Q5-8 | × persistence/reopen (§I.10): `.tc-panel` open/closed persistence keys by id; `refPanel` joins, nested disclosures behave like evidence sub-panels. No conflict with default-open. | holds | — verify by deed in step 8 |
| Q5-9 | × gap hierarchy (DS-INV-9 slice, s57): nested-panel `--gap` spacing — is the shipped selector `#evidence`-specific? If so the fix was pointwise; widen to ANY nested tc-panel (incl. `#refPanel`'s) — the fix-the-class rule. | must-fix (code check) | FOLDED — checked in code step; widen selector if evidence-specific |
| Q5-10 | Empty state: directions defined, none close ⇒ one-line "no close direction yet"; shipped code prints the forbidden siblings phrase "No similar tracks" (D-INV-22 vocabulary leak). | must-fix (code+test, pre-existing bug caught by the walk) | FOLDED — spec'd in §D.10.1; red-on-bug test queued |
| Q5-11 | One-source (§D.10.2): all ≤3 per-direction web bodies render from `reference_web_notes.json` via the same renderer; data already covers all 3 directions (verified on disk). Side page (N10) untouched. | holds | — |
| Q5-12 | Catalog URL entry-focus ("opens focused on that direction") stays an unwired 0.9.x promise; the merged selector remains its hook — merge neither wires nor regresses it. | holds (out of scope) | — |
| Q5-13 | ★/☆ bar marks are already per-direction (conf per direction per refpanel) — merge doesn't touch them. | holds | — |

Open ⟨DECIDE⟩ touched by this surface: none new; ⟨DECIDE D-25⟩ (Simple chip) and ⟨DECIDE D-31⟩
(per-your-track shared mark) remain open, untouched by the merge.

Must-fixes: 6 (Q5-1..4, 7, 9, 10 — Q5-9 conditional on the selector check) — ALL folded into the
spec in this landing; their test/code halves land in the matrix→test→code steps of the same feature.

## MINOR-gate record (1.0.2 → 1.1.0, appended after code landed)

The method wants the 3-pass preventive audit before a MINOR. The FULL 3-pass gate ran SAME-DAY at
1.0.0 (s54–s56: `2026-07-05.md`, `-pass2.md`, `-pass3.md` — all closed). Delta since that gate:
1.0.1/1.0.2 (in-panel cosmetics, own tests) + THIS merge. The gate is therefore taken as the
same-day full pass PLUS this delta walk:
- pass-1 delta (spec prove): this record's Q5-1..13, all folded, each with an owning test.
- pass-2 delta (matrix audit): the s58 rows landed with the change; `test_traceability` green,
  now INCLUDING the new reverse matrix→SPEC gate (morning landing, same session).
- pass-3 delta (surface composition): D-INV-36 × {view ladder, references switch, viewport,
  persistence/reopen, gap hierarchy, INV-47 empty-open, quick absence} — every cell has a
  browser-level owning test (`MergedReferencePanel`, gate test_22/23/24, ladder/hide-set tests).
Suite at bump: **772 passed / 2 expected skips.** Verdict: MINOR gate satisfied; a from-scratch
3-pass re-walk would re-audit surfaces untouched since this morning's gate.

Q5-9 resolution (by deed): the shipped nested-gap selector WAS `#evidence>details.tc-panel`
(per-id) — widened to `#evidence>details.tc-panel,#refPanel>details.tc-panel` with the matrix row
rewritten structurally; `PanelGapHierarchy` browser test stays green.
