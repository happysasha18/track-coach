# Prover record — 2026-07-05 (late) — CROSS-LINK: "The arc answers back" (§B.13, INV-49a–d)

Mode: CROSS-LINK (single added interaction on an existing surface — the arc canvas hit-zone).
Scope: the new block's seams against §B.13/INV-48 (card→panel), §B.14 seek (INV-33/38), the cue
triangles + hover tooltips, the view ladder (§B.15/INV-41). Shipped-system pins verified by command,
not prose: `cueAt` at `scripts/build_widget.py:3754`, `flashRec` def `:3467` (single caller `:3768`),
triangle draw `:3718`, mousemove/click wiring `:3757–3770`.

## Opening assessment

The delta is small and honestly scoped: it widens an EXISTING hit-zone (`cueAt`'s y-band) rather than
adding a second wiring, and the spec says so (INV-49b "one hit-test serves band and column"). Fences
name the right neighbours, and the trade-off (cue tooltip displaces the generic readout inside the
column) is stated instead of smuggled. Two small things needed tightening — the word "chart" left the
column's vertical extent ambiguous, and a stale code comment claims a second affordance that does not
exist. Ready to build after AB-1 folds.

## Findings

| id | finding | severity · label | folded / rejected |
|---|---|---|---|
| AB-1 | "at ANY height over the chart" is ambiguous — the canvas below `famBot()` holds the family rows and the time-axis strip; is a click there inside the column? A reader can defend either. Consequence: the test author and the implementer can pick different y-bounds and both cite the spec; the browser test then pins the wrong promise. Action: one rule — the column spans the WHOLE canvas; say "anywhere on the arc canvas" in INV-49a. `should-clarify · over-general (abstraction)` | should-clarify | FOLDED — INV-49a now reads "at any height on the arc canvas (the full canvas, family rows and axis strip included)". |
| AB-2 | Stale comment names a second affordance: `build_widget.py:3466` says flashRec is "used when a timeline triangle / cue-index item is tapped", but the only caller is the canvas click (`:3768`); no cue-index list exists in the shipped widget. Consequence: the next reader (or a worker with a brief) hunts for a cue-index surface that isn't there. Action: reword the comment to name the real caller(s) in the same change. `worth-considering · hard-to-operate (ops-ux)` | worth-considering | FOLDED — comment reworded with the code change. |
| AB-3 | Touch inside the column has no preview (no hover): the tap seeks AND scrolls away to the card unannounced. Checked against the spec: the drawn triangle at the column's x remains the visible mark, and the block already tags the tooltip "advisory, never the sole affordance [default]". Same family as today's band on touch, only easier to hit. No action — recorded so the choice is visible. `worth-considering · confusing-for-users (cognitive-load)` | recorded, no action (spec'd [default]) |

## Seam walk (both sides present and named the same)

- **§B.14 seek:** cue click seeks to the CUE'S moment (`window.__seek(cue.t)`, `:3768`) — stated in
  INV-49b; non-cue click seeks to the exact clicked time — INV-49c. Seek-preserves-playback is owned by
  `seekResult` (INV-33/38) and is upstream of this change. CLEAN.
- **INV-48 forward navigation:** card→panel wiring is `#recs`-scoped and untouched; the backpointer ends
  at `flashRec` (card flash + scroll), which never enters the panel-navigation path. CLEAN.
- **View ladder (INV-41):** cues exist only for `t != null` cards, which Simple shows (INV-22 hides
  global cards only) — the lit card is present in both views; no view state is read or written. CLEAN.
- **Tooltips:** inside the column the cue tip displaces the time·scene·playing readout — spec'd as a
  chosen trade-off `[default]`; the readout returns outside the snap radius. CLEAN (named, not hidden).
- **INV-34 (pulse/flash is CSS/DOM-only):** the change is hit-testing only; no drawing code edited. CLEAN.
- **Persistence/two-windows:** INV-49d, nothing written. CLEAN.

## Coverage notes (CROSS-LINK scope)

Ambiguity/ties: two cues < 2×radius apart → nearest wins, first-in-time on a tie (existing `cueAt`
iteration order) — deterministic, unchanged. Bounds: no timecoded cards ⇒ no cues ⇒ INV-49c everywhere;
cue at t≈0 puts the column over the left label margin — same snap family, accepted. Dependency,
reference integrity: `flashRec` no-ops on a missing `data-let` target (`:3467-3468` early return) — no
dead click even if a card were absent. Unbacked surfaces: none added — no new DOM, no new drawn marks.

Previous record check: `2026-07-05-cardnav.md` — no unfolded must-fix rows (CN-8 recorded as a
non-goal note, stands).

Verdict: ready to build. MINOR-gate note: the 1.0 3-pass audit stands; this delta is CROSS-LINK-proven
(the s59/s61 precedent for post-1.0 MINORs).
