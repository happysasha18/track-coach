# Prover record — card→evidence navigation (INV-48, §B.13), 2026-07-05

Mode: CROSS-LINK (new interaction on existing surfaces; precedent = 1.2.0 entry-focus). Delta: §B.13
"A card leads to ITS evidence" block (INV-48a–e) against named neighbours §B.14 (player), §B.15/INV-22
(view ladder), INV-31 (based-on), INV-34 (pulse), D-INV-37 (entry-focus), INV-47 (empty drawer).

Previous record `2026-07-05-entryfocus.md`: no unfolded must-fix rows (checked this session).

| # | Finding | Verdict | Folded / rejected |
|---|---|---|---|
| CN-1 | `#vitals` is a plain `div.vitals`, not `details.tc-panel`; shipped pulse CSS is `#storyPanel.pulse` only (build_widget.py:3233). "One shared pulse style" (INV-48b) requires a generic `.pulse` rule that renders on both panel kinds. | must-fix (spec→code shape) | folded — implement one `.pulse` selector for every target; browser-level matrix row asserts the pulse animation on a NON-story target |
| CN-2 | `#rhyPanel`/`#autoPanel` live inside the collapsed `#evidence` drawer; canvases in a closed `<details>` have zero layout. The drawer already carries toggle listeners that resize/redraw on open (3576/3784/3882), and programmatic `.open = true` fires `toggle`. INV-48c holds only through that seam. | must-fix (test level) | folded — the INV-48c browser test asserts the target panel is visible with non-zero height after a card click, not just scrolled |
| CN-3 | The rec tuple `(cls, when, head, body, fix, t, based, axis)` carries no rec KEY, so `ev` cannot be derived late — it must attach at `add()`-time from a `REC_TARGET` dict keyed like `REC_BASED`, and `per_stem_cards` sets its own. Machine check for INV-48a = every key with a card has a target (same completeness rule as based-on). | must-fix (shape) | folded — REC_TARGET beside REC_BASED + a unit twin of the based-on completeness test |
| CN-4 | Simple × the target map: Simple hides only `#stemlanes`/`#seqKey`/`#refPanel`/global cards (CSS 3026/3029/3121) — NO target in the map is Simple-hidden (`#stemlanes` sits inside `#storyPanel`, whose container always shows). The spec's composition sentence is true against shipped CSS. | holds | — |
| CN-5 | Quick run: a global swing card may render while `#rhyPanel` is absent → INV-48d makes it not clickable; every timecoded card in quick targets `#storyPanel`, which quick has. No dead-click path exists. | holds | folded — negative-side matrix row for INV-48d |
| CN-6 | D-INV-37 entry reader scrolls `#refPanel` once on load; a card click after load pulses another panel. Different triggers, no shared state — no interaction. | holds | — |
| CN-7 | `masking_clean` ("no overlap found", global) becomes clickable → `#storyPanel`. Marginal user value, but the alternative (a special not-clickable case) adds a rule for no gain. | accepted, recorded | — |
| CN-8 | **The `.rec` class is worn by NON-recs too:** the map-panel note cards (export/model/leakage, `mapNotes`) are built as `<div class="rec do">` but are NOT `D.recs` and are out of §B.13 scope (F2). Click wiring must stay scoped to `#recs .rec` — a bare `.rec` selector would make them clickable. | must-fix (fence sharpened) | folded — wiring stays `#recs`-scoped (as shipped today, 3615); a test asserts map-panel note cards carry no pointer/jump |

Architecture lens: INV-48a owned by N15 (card evidence — REC_TARGET lives beside REC_BASED); INV-48b/c/d/e
owned by N13 (player/card-click state machine JS). New seam content: the `ev` field in `D.recs`
(N15 → N13). No new node; no structure change → architecture prove = description + pin update only.

MINOR-gate delta note: additive feature on two existing nodes; the FULL 3-pass ran at the 1.0 gate
(2026-07-05{,-pass2,-pass3}.md); per the 1.2.0 precedent a CROSS-LINK suffices for this MINOR.

Open ⟨DECIDE⟩ touched by these surfaces: none (the §B card ⟨DECIDE⟩s — divergence τ/ρ, budgets, sort
default — are untouched by navigation). No unfolded must-fix remains.
