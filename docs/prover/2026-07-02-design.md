# product-prover — SPEC §I visual design system (CROSS-LINK mode, 2026-07-02)

Reviewed the new §I (tokens & components) against its cross-links (view-ladder, §B.14 player,
catalog builder, §D.10 reference read). Findings + fold status:

| id | headline | severity | folded? |
|---|---|---|---|
| F1 | dedup guard can't be written from a hex value — stems & UI tokens are the same string; key it by LOCATION | must-fix | FOLDED into DS-INV-7c (guard scans CSS/`:root` only; data-viz sites whitelisted by location) |
| F2 | I.2 grid change contradicts I.10 "existing browser tests pass unchanged" | must-fix | FOLDED — DS-INV-8 states it REPLACES the s34 recs container-query; column-count tests are UPDATED, not held; I.10 now distinguishes visibility (held) vs layout counts (updated) |
| F3 | "expansion animates height" names a primitive CSS can't deliver (height:auto) | must-fix | FOLDED into I.7 #3 (`grid-template-rows: 0fr→1fr`; restored-open renders expanded, no first-frame zero-height) |
| F4 | text-ladder collapse had no per-hex mapping rule | should-clarify | FOLDED into DS-INV-3 (explicit per-hex → rung table; `--ink-dim` added to widget + catalog) |
| F5 | segmented merge visibly changes the view-toggle & reftab selected state | should-clarify | FOLDED into DS-INV-13 (flagged as intended visible change; screenshot before/after) |
| F6 | `#0c0e14`/`rgba(167,139,250)` are raw literals duplicating `--bg`/`--wob` inside the ladder | should-clarify | FOLDED — I.4/I.6 now use `var(--bg)` + `color-mix(--wob)` for the focus ring |

Acknowledged gaps (correctly deferred, not folded):
- DS-1 — §I.8 typography weight placement → Alexander's call (audit + safe snaps land; weights held).
- DS-2 — catalog `_SIM_COL` (#2e9e5b/#d8932a/#c2503d) is a separate muted similarity palette; LEAVE.
  Extra rule to preserve if ever unified: the reference column's "far" is deliberately grey (no red),
  sibling "far" is red (`catalog.py:37-38`).

Landed this session (movement 1): F1/F4 fixes are live in code — DS-INV-1/2/3/7c guarded by
`tests/test_design_tokens.py` (green). F2/F3/F5 apply to §5/§6/§7, folded in spec, coded next movement.
