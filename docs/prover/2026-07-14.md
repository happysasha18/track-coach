# Prover + fresh-eyes audit — 2026-07-14 (s69, RC-INV-13f / 1.7.1)

Scope: the failed-run honest-placeholder feature (RC-INV-13f, decision E-4). CROSS-LINK self-prove of the
new clause against §E neighbours, plus an independent adversarial audit from a fresh context (hypothesis
"tasks completed, goal missed"). Change shipped as 1.7.1, pushed `47c8757`.

## Self-prove (CROSS-LINK, in-context)
Seam analyzer→render via `run_meta.json` `analysis_state`; both sides present and named the same. Checked
composition with auto-redo (RC-INV-13a), deposit gate (G-INV-17), completeness line (RC-INV-13b), reference
runs (RC-INV-13e). One refinement folded before build: the analyzer clears any stale `failed` marker at the
start of a fresh attempt (stamps "running"), so a re-run that then succeeds never shows the failure message.

## Fresh-eyes audit — findings

| # | Kind | Finding | Disposition |
|---|---|---|---|
| 1 | recommendation | The honest render needs `result_core.json`, so an EARLY failure ("a source it cannot read", before core is measured) never reaches it — analyze exits loud with no widget, and a manual `build` on that coreless dir crashes at build_widget:4614 rather than rendering the failed placeholder. The false-*reload*-promise bug is still fixed for the case it occurred (mid/late failure with a built widget). | FOLDED into spec: RC-INV-13f now names the boundary honestly and marks the no-core honest widget a queued follow-up. Not a regression. |
| 2 | recommendation | `cmd_analyze` builds no widget; the honest surface depends on a separate `build` on the failed dir. Spec now says "when a widget is built for such a run". | FOLDED (spec wording). |
| 3 | verified sound | Hard kill (kill -9 / OOM) the analyzer can't catch leaves "running" → keeps the recoverable placeholder. Matches E-4's explicit scope; a killed run would complete on re-run. | No action — spec now states it. |
| 4 | verified sound | Marker coverage: `audio not found` exits before the run dir exists (no surface to mislead); every in-pipeline failure from step 1 on is inside the `try/except BaseException`, so `SystemExit` from `Runner._run`'s `sys.exit` is caught, stamped, re-raised with the same non-zero code. | No action. |
| 5 | verified sound | Stale-marker inertness: `_read_analysis_state` is read at exactly one site, strictly inside `if not V.is_valid(...)`. A valid run with a stale `failed` marker renders the full widget (tested). | No action. |
| 6 | recommendation (pre-existing) | `_FAILED_STATUS`/`_FAILED_HINT` are hardcoded module constants despite the `[default]` tag, mirroring the pre-existing `_INCOMPLETE_STATUS` — neither is plumbed to a `--strings` override. Consistency debt shared with the incomplete placeholder, not introduced here. | QUEUED. |
| 7 | verified sound | Tests assert real `run_meta.json` and real rendered HTML (not mocks); red-first credible. Traceability green (RC-INV-13f matrix row cites six real tests; `RC-INV-*` is outside the enumerate-namespace, so no Formal-index row owed). | No action. |
| 8 | recommendation | ARCHITECTURE N19 states L3-BROWSER for "shows the gap honestly", but the static failed placeholder is honestly leveled L1-DOMTEXT in the matrix row. Minor doc-level nit; a browser test adds little for static HTML. | QUEUED. |

No DEFECTS. All findings folded or queued. Verify-by-deed: three real widgets rendered and eyeballed
(failed → honest message + hint; interrupted → bare reload line; valid → full widget, marker inert).
