# Prover run — 2026-07-02 (CROSS-LINK)

Surface under review: reference marker (§D.3) + ingest guard G-INV-18 (§G) + ⟨DECIDE G-3⟩.
Mode: CROSS-LINK — seams of the new marker/guard against Deposit (G-INV-17), library index
(G-INV-14/16), gc/reset (G-INV-10/15), reference layer (D-INV-3/7/20), centroid gen
(`gen_reference_directions.py`).

| ID | Severity | Headline | Folded? |
|----|----------|----------|---------|
| F1 | must-fix | gc prunes never-deposited reference run dirs → destroys fingerprints `gen_reference_directions.py` regenerates directions from | FOLDED — added G-INV-19 (reference run dirs gc-exempt) |
| F2 | must-fix | Guard is going-forward only; 25 already-leaked entries have no marker, need path/list-based one-off removal + index backup | FOLDED — added §G.4 one-off cleanup paragraph (identify by source-album path, backup index) |
| F3 | should-clarify | Ensure guard sits at shared deposit boundary (`deposit_from_run`, has `meta`), not only auto path | FOLDED — G-INV-18 prose already names both paths; noted implementation home in matrix (guard in `deposit_from_run`) |
| F4 | should-clarify | Marker provenance unstated — no action sets `reference: true` | FOLDED — §D.3 now names the `--reference [--artist]` analyze option as the setter |
| G-3 | acknowledged | Where reference runs live instead of library — tab deferred, but gc-exemption pulled into this pass | RESOLVED for gc (F1); tab stays deferred |

Phase-4 note (observability): migrate banner counts library members only → becomes honest once
references excluded. Added one-line note to §G.

Open ⟨DECIDE⟩ after this pass: G-3 (separate reference-catalog tab vs nowhere) — deferred to the
tab pass; gc-exemption resolved now.
