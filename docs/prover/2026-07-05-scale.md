# Prover record — 2026-07-05 (late) — CROSS-LINK: "A number wears its scale" (§B.16, INV-50a–c)

Mode: CROSS-LINK (text-only change to three existing card bodies; no new surface, no new state).
Scope: §B.16 against INV-31 (based-on untouched), the §B.11 sort law, the golden real-data fixtures,
and the rec-template table (`build_widget.py` `"recs":233`, swing add `:1441`, squashed `:1450`,
tonal `:1464`).

## Opening assessment

The delta is the right size for a closing pass: three named bodies, the scale in the sentence, two of
the three phrases computed from the measurement so the fix doesn't re-template (the exact trap the
signal map warns about). The class sweep (below) confirms the three ARE the whole class. One finding
needed folding (WD-1); the rest is clean.

## Findings

| id | finding | severity · label | folded / rejected |
|---|---|---|---|
| WD-1 | The success measure says "verified by deed at landing" but the three cards fire on DIFFERENT library tracks (swing needs >30 ms, tonal needs a ≥4 dB band, squashed needs DR <6) — no single track shows all three. Consequence: a one-track deed under-verifies and the landing report claims more than was seen. Action: the deed enumerates which track evidenced which card (or uses the synthetic fixtures where a real track lacks the card, LABELLED synthetic per the show-real rule). `should-clarify · missing-outcome-check (postcondition)` | should-clarify | FOLDED — deed plan: per-card evidence list, real tracks first, synthetic labelled fallback. |
| WD-2 | Class sweep (sibling-instances lens): walked every `R["recs"]` body + per-stem card texts for bare numbers without an in-sentence scale. Relative already: long_section (%), plateau (tail %), climax (70% norm), truepeak (0 dBTP ceiling), masking (% of track), breakdown (% in), wobble (start→end pair), per-stem cards (vs the rest). No numbers: energy_flat, endpoint, empty_stem, low_carrier, bass_groupstem, stem_evolves, late_entry, intention_result (times only). The class is EXACTLY the three named. No fourth instance. | verified, no action |
| WD-3 | Perceptual honesty of the tonal phrase steps: +10 dB ≈ twice as loud (perceived), so "≥9 ≈ about twice as loud" and the −9 mirror "about half as loud" are defensible roundings; ≥6 "half again" and ≥4 "clearly audible" sit inside common loudness-JND practice. The phrase is a READING AID, not a measurement claim — the measured dB stays in the sentence beside it (credibility layer holds: we still say only what we measured, the phrase translates it). | verified, no action |
| WD-4 | Boundary determinism: swing fires only at sw>30 (gate `:1440`), bands [30,60)/[60,90)/[90,∞) cover the whole domain with no gap/overlap; tonal fires at \|dev\|≥4, steps [4,6)/[6,9)/[9,∞) likewise, sign picks the mirror. No tie ambiguity. | verified, no action |

## Seam walk

- **INV-31 (based-on):** REC_BASED untouched by the delta — fence holds by construction; the golden
  fixture test asserting non-empty based-on is unaffected. CLEAN.
- **Sort law (§B.11 `_rank`):** REC_CLASS untouched; text changes cannot reorder. CLEAN.
- **Golden fixtures / render tests:** swept `tests/` for pins on the three bodies — none pin the prose
  (only `data-ev` selectors reference these cards). New rows are additive; no test is edited to pass. CLEAN.
- **View ladder:** bodies identical across views (ladder gates WHICH cards, never text) — already the
  standing INV-22 shape. CLEAN.

Previous record check: `2026-07-05-arcback.md` — no unfolded rows (AB-1/2 folded, AB-3 recorded-by-design).

Verdict: ready to build. PATCH-scale change (wording refinement under a new spec clause; no new
surface/interaction) — 1.4.1.
