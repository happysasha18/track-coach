# Surface-composition — 1.0 GATE, pass 3 of 3

**Date:** 2026-07-05 (s56). **Input:** every stateful surface in `docs/SPEC.md` × the canonical axis list.
**Mode:** FULL. **Purpose:** the third pre-1.0 preventive-maintenance pass (whole-spec prove → matrix audit →
**surface-composition**). Pass 1 = `2026-07-05.md` (F1–F5 closed s55); pass 2 = `2026-07-05-pass2.md`
(P2-1/P2-2 closed s56).

## Method

The canonical axis list (livespec / spec-author): **view · mode · tier(data-state) · viewport size ·
persistence/reopen · concurrency**. For each stateful surface, walk every axis cell and confirm it is
covered (spec sentence + owning test) or name the gap. Pass 1 already found the composition "explicit, not
implied" — this pass is the exhaustive cell-by-cell confirmation plus a search for the one unstated cell.

## The walk

| Surface | view | mode | tier/data | viewport | persist/reopen | concurrency |
|---|---|---|---|---|---|---|
| S1 element grid | §5 F-S/F-D/Q ✓ | §5 ✓ | §5 data-state ✓ | recs grid reflow (DS-INV-8, 460/720/1100) ✓ | §I.10 tc-panel + view ✓ | n/a |
| Player | §B.14 mix resets on view ✓ | per-stem vs mix (INV-7) ✓ | no audio ⇒ no player ✓ | within grid ✓ | view-toggle resets mix ✓ | one player ✓ |
| View selector | INV-3/19/22 ladder ✓ | quick = hint ✓ | n/a | segmented ctrl in `--w-content` ✓ | §B.15 remembered + first-calm + hash ✓ | one global view ✓ |
| Reference read / web panel | INV-18/22 Detailed-only ✓ | D-INV-20 quick absent ✓ | F1 `when-reference` ✓ | **P3-1 (was unstated) → CLOSED** | tab ephemeral D-INV-28 ✓; tc-panel collapse ✓ | one widget ✓ |
| Catalog (S2) | §6 ✓ | mode pill INV-20 ✓ | per-metric dash ✓ | column-shed INV-10 (1100/880) ✓ | newest-only D-INV-35 ✓ | one preview INV-14 ✓ |
| Evidence drawer | INV-18 every view ✓ | INV-47 quick hides container ✓ | sub-panels gate INV-16 ✓ | within grid ✓ | tc-panel ✓ | n/a |
| Completeness gate | INV-47 {S,D,quick} ✓ | INV-47 ✓ | INV-GATE data-states ✓ | n/a (net) | n/a | n/a |
| Design tokens | §I.10 × view ✓ | ✓ | n/a | §I.10 × viewport ✓ | §I.10 × persist ✓ | n/a |

Every cell resolves except one, found and closed:

## Finding

### P3-1 — the §D reference surfaces were composed across every axis EXCEPT viewport; CLOSED

`#refRead` / `#webPanel` and the up-to-three `.reftab` selector were composed across view (Detailed-only,
INV-18/22), mode (quick-absent, D-INV-20), tier (`when-reference`, F1), persistence (tab ephemeral D-INV-28 +
tc-panel collapse) and concurrency (one widget). The one axis they were **not** stated or tested on was
**viewport size**: SPEC §I.10 `× viewport` named only the recs grid + segmented control + cards, and every
`test_headless_render` ref probe ran at `width=1100`. A narrow-screen horizontal overflow (a panel past the
viewport edge, an internal h-scroll, or a 3-tab row spilling) would pass every string test and every wide
browser test.

**Verified empirically first** (probe at the harness's narrowest effective viewport, ~500 px): `#refRead` and
`#webPanel` sit at right=472 inside vw=500 (28 px margin), `scrollWidth == clientWidth` (no internal
h-scroll), and a 3-tab selector reaches right=343 (157 px margin) — the cell is **clean**, no code bug. Then
**closed the cell**: SPEC §I.10 `× viewport` now names the §D surfaces; matrix gained a `§I.10 ×viewport
(§D refs)` L3-BROWSER row; new guard `RefReadSurfacesRendered::test_ref_panels_stay_within_viewport_when_narrow`
asserts panel containment (always) + tab-row fit (when a multi-direction selector renders). No code change
(behaviour already correct) ⇒ no version bump; suite +1 (763→764). `worth-considering · boundary-issue
(composition)` — **CLOSED.**

## Verdict

**Pass 3 GREEN.** Every stateful surface is composed across the full canonical axis list; the single unstated
cell (§D refs × viewport) is now stated, empirically clean, and guarded browser-level. With pass 1 (F1–F5)
and pass 2 (P2-1/P2-2) closed, the **3-pass 1.0 preventive-maintenance gate is complete and green** — no
must-fix open. The remaining 1.0 items are the end-to-end orchestration run (NEXT_STEPS item 4) and Alexander's
explicit word for the MAJOR 1.0.0 bump; the spacing-token design pass (F5) stays deferred POST-1.0 (his call).
