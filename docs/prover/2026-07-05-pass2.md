# Matrix audit — 1.0 GATE, pass 2 of 3

**Date:** 2026-07-05 (s56). **Input:** `docs/TEST_MATRIX.md` (545 lines) projected through `docs/ARCHITECTURE.md`
(24 nodes) against `docs/SPEC.md`. **Mode:** FULL. **Purpose:** the second of the three pre-1.0
preventive-maintenance passes (whole-spec prove → **matrix audit** → surface-composition). Pass 1 =
`docs/prover/2026-07-05.md` (F1–F5 closed s55, F6 carried here). Pass 3 = surface-composition, next.

## Method

The matrix audit asks the four architecture rework-questions (`ARCHITECTURE.md` §"What this doc lets us ask"):
Q1 every node has ≥1 owning test; Q2 every rendered node has L3-BROWSER for its visibility/layout facts; Q3
every SPEC invariant projects to a row pinned to the RIGHT level; Q4 positive AND negative per fact. Split by
labour: the **mechanical** half (run the whole suite; confirm every owning-test the matrix NAMES actually
exists) was delegated to a sonnet-worker — raw output in `docs/prover/pass2_worker_checkpoint.md`; the
**judgment** half (level-correctness, fact-ownership, coverage completeness) done here on Opus.

## Empirical result (worker, raw in checkpoint — spot-checked by re-run)

- **Suite: 763 passed / 2 skipped / 0 failed, exit 0.** The 2 skips are `INV-29`/`INV-30` (source-file header
  symmetry, PROPOSED-not-built, `@unittest.skip`) — the skip-set is EXACTLY the expected list. Green.
- **Matrix→test existence: 1 orphan of ~109 method-refs + 103 class-refs.** `RC-INV-2` named
  `test_completeness::MissingIsNotZero::manifest`; the real node is `::test_manifest_lists_only_measured_axes`
  (the test exists + passes — a stale LABEL, not a missing test). **FIXED s56** (matrix line 209). Every other
  reference across all 20 cited test files resolves. Spot-checked by re-running the class (3 passed).
- Inventory: 26 test files, ~766 `def test_` methods.

## Findings

### P2-1 (fact-ownership, from F6) — the web-panel/★ render was double-owned; FIXED

F6 (pass 1) flagged the arch pins as file-level, not `file:line`, so "one fact → one node → owning line"
couldn't be walked. Closing it exposed a real drift: `ARCHITECTURE.md` **N10** claimed to own "the web panel
+ ★ cross-validation **render**" with owning code `build_reference_notes.py` — but the render functions
(`render_reference_notes`, `_web_panel_html`, `render_reference_read`, `_refread_bars_html`,
`_ref_read_html`) are all defined in `build_widget.py`, which is **N17**'s territory. `build_reference_notes.py`
is actually a SEPARATE surface — a standalone light-theme §D.10.2 side-page (`build:198`, writes
`~/.track-coach/explore/reference_notes.html`) that REUSES the N17 renderer.

**FIXED s56:** (a) N8/N12–N17 pinned to owning `file:line` (function names + lines from grep); (b) N10
rescoped to "reference-notes DATA + the standalone side-page that reuses the N17 renderer — NOT the in-widget
render," pinned to `build_reference_notes.py:build:198` + `data/reference_web_notes.json`; (c) N17 pinned to
its five `render_reference_*`/`_web_panel_html`/`_ref*` functions in `build_widget.py`. One fact → one node,
now walkable. `must-fix (was worth-considering) · boundary-issue (composition)` — **CLOSED.**

### P2-2 (coverage checklist freshness) — §8 was dated s12; refreshed to 1.0 scope

The matrix's coverage-status block (§8) — the "is every invariant rostered?" home — was last walked at **s12
(2026-06-23)**, before INV-42..47, INV-GATE, the §D shipping surfaces (F1), and the §G/§H/§I ext-namespace
rows existed. For a MAJOR gate whose deliverable is "coverage-validation checklist, walked," a stale checklist
under-states what is covered. **FIXED s56:** §8 gained a "1.0-gate coverage walk (s56)" block answering the
four arch rework-questions with the current invariant set. `should-clarify · discharge` — **CLOSED.**

## Acknowledged (not blockers)

- **N2 (Demucs separation) and N9 (reference directions) have no DIRECT unit test — tested "indirect via
  fixtures."** Both need real audio / a Demucs run to exercise directly; a synthetic direct test would assert
  a mock, not the behaviour. Indirect-via-fixture coverage is the honest level here. `acknowledged`
- **INV-25 (Simple-promotion) / INV-26 (sort toggle) genuinely not built** — deferred feature, correctly
  carried as "planned," not a false-covered row. `acknowledged` (matches SPEC/JOURNAL).

## Verdict

**Pass 2 GREEN.** The matrix is a faithful, level-correct, fully-rostered projection of the spec through the
architecture: levels are right (every visibility/layout/colour fact at L3-BROWSER), every named owning-test
exists (1 stale label fixed), the 11 previously-baselined ext-namespace invariants all have rows, and
fact-ownership is now walkable with the one real drift (P2-1) reconciled. No must-fix left open. Proceed to
pass 3 (surface-composition).
